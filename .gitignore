# Local environment
.env
.env.local
.env.*.local
.secrets
# User-specific config copies (keep examples, ignore active config)
config/node.conf
config/pool.env
config/miner.env
config/bdag-exporter.env
snapshots/
# Editor / OS
*.swp
.DS_Store

# Local volumes if anyone bind-mounts them here
data/
