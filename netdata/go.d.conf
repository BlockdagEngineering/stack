# Merged with image defaults. Stock Docker SD uses user netdata/password postgres;
# pool Postgres only defines POSTGRES_* in .env, so disable these modules to avoid log noise.
enabled: yes
default_run: yes
modules:
  postgres: no
  pgbouncer: no
  prometheus: yes
